===========================
PROMPT 0001

- update the game so that the bosses are tiered 0-1-2-3
- change the phasing so that it triggers when the boss hits 0 hp instead of every 33%, but heals back up to full hp instead of dying. there should be some sort of indicator next to the boss's healthbar that shows how many lives it has left (phases). so it would start with 2 stars, then you'd phase it and it would go to 1 star, then you kill it again and it goes to 0 stars. this is similar to dark souls. 
- tier 0 is what we have now. it should have just 2 phases. phase 2 should introduce the ground slam. the ground slam should be a giant AOE that the player can dodge by jumping. 
      - this boss will not have a phase 3 in tier 0. 
- tier 1 we will implement next, and it will add an additional phase to the boss with more abilities. hold off on implementing this until we discuss and plan it further. 
- update the readme to include this new information and clean up any outdated information.
- add a menu that pops up when you access the portal from the home town. this menu will be used to select the map. currently we just have the one map. the player should be able to select a map, and select a tier for the map, and then hit go and that triggers the portal teleportation to that map. 

evaluate this request and spawn up to 5 sonnet subagents to complete isolated tasks. one of the subagents should be a code reviewer, making sure we're consistent with other code in the project. 
===========================